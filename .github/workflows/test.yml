name: ci
on:
  push: 
    branches: [main, development, v1*]
  pull_request:
    branches: [main, development, v1*]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Build site
        run: bundle exec jekyll build
      - name: Check JavaScript syntax
        run: |
          for file in assets/js/*.js assets/js/explore/*.js assets/js/explore/core/*.js assets/js/explore/modules/*.js assets/js/explore/components/*.js netlify/functions/*.js; do
            [ -e "$file" ] || continue
            node --check "$file"
          done
