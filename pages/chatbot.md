---
layout: page
show_title: false
permalink: /chatbot/
---

<div class="container">
  <h1>Ask about the Unknown Hands corpus</h1>
  <p>Ask analytical questions about the medieval manuscripts, scribes, and colophons in our corpus.</p>
  <p class="chatbot-editorial-note"><strong>Verification note:</strong> Please verify AI-generated conclusions against the referenced manuscripts, as automated synthesis may contain errors.</p>
  
  <div class="chatbot-container" style="max-width: 900px; margin: 0 auto; display: flex; flex-direction: column;">
    
    <!-- Chat log -->
    <div id="chat-messages">
      
      <div class="bot-message">
        <strong>Assistant:</strong> I can retrieve and summarize indexed colophon material.<br><br>
        
        <strong>Example questions:</strong>
        <ul style="font-size: 14px; margin-top: 5px; margin-bottom: 0;">
          <li>"How do these female scribes express humility?"</li>
          <li>"How do these scribes describe their sins?"</li>
          <li>"Summarize how the scribes talk about prayer."</li>
        </ul>
      </div>

    </div>
    
    <!-- Input area -->
    <div class="chatbot-input-row">
      <input 
        type="text" 
        id="user-input" 
        placeholder="Ask a question about the colophons..." 
        onkeypress="if(event.key === 'Enter') sendMessage()"
      >
      <button 
        onclick="sendMessage()" 
        class="chatbot-send-btn"
      >
        Send
      </button>
    </div>
    
    <div id="loading-indicator" style="display: none; text-align: center; margin-top: 10px; color: #666; font-style: italic;">
      <span>Retrieving sources…</span>
    </div>

  </div>
</div>

<style>
.chatbot-editorial-note {
  margin: 1rem 0 1.5rem;
  padding: 0.8rem 0;
  color: #4b5260;
  border-top: 1px solid #9ca8ba;
  border-bottom: 1px solid #e4e7ec;
  font-size: 0.86rem;
  line-height: 1.55;
}
.chatbot-editorial-note strong {
  margin-right: 0.35rem;
  color: #303847;
  font-size: 0.76rem;
  letter-spacing: 0.055em;
  text-transform: uppercase;
}
.bot-message {
  margin-bottom: 15px;
  padding: 12px;
  background-color: #f5f6f8;
  border-left: 2px solid #9ca8ba;
  border-radius: 2px;
  line-height: 1.5;
}

.user-message {
  margin-bottom: 15px;
  padding: 12px;
  background-color: #f5f5f5;
  border-radius: 2px;
  text-align: right;
  font-weight: 500;
}

.rendered-markdown p {
  margin-bottom: 10px;
}
.rendered-markdown ul {
  padding-left: 20px;
  margin-bottom: 10px;
}
.rendered-markdown blockquote {
  border-left: 4px solid #ccc;
  margin: 10px 0;
  padding-left: 10px;
  font-style: italic;
  color: #555;
}

.source-badges {
  margin-top: 15px;
  padding-top: 10px;
  border-top: 1px dashed #b3d4fc;
  font-size: 12px;
}
.source-badge {
  display: inline-block;
  background-color: #f5f6f8;
  color: #445064;
  border: 1px solid #d8dde5;
  padding: 3px 8px;
  border-radius: 2px;
  margin-right: 5px;
  margin-bottom: 5px;
}
#chat-messages {
  border: 1px solid #d8dde5;
  border-radius: 2px;
  padding: 1.25rem;
  min-height: 400px;
  max-height: 600px;
  overflow-y: auto;
  margin-bottom: 1rem;
  background: #fff;
}
.chatbot-input-row {
  display: flex;
  gap: 0.625rem;
}
#user-input {
  flex: 1;
  padding: 0.75rem;
  border: 1px solid #c8ced8;
  border-radius: 2px;
  font-size: 1rem;
}
.chatbot-send-btn {
  padding: 0.75rem 1.5rem;
  background: #9a6b08;
  color: #fff;
  border: 1px solid #9a6b08;
  border-radius: 2px;
  cursor: pointer;
  font-size: 1rem;
  font-weight: 600;
}
.chatbot-send-btn:hover,
.chatbot-send-btn:focus-visible {
  background: #785305;
  border-color: #785305;
}
</style>

<!-- Load marked.js and DOMPurify to render sanitized markdown generated by Gemini -->
<script src="https://cdn.jsdelivr.net/npm/marked@12.0.2/marked.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/dompurify@3.1.6/dist/purify.min.js"></script>

<script>
const RAG_API_URL = window.location.hostname.includes('netlify.app') || window.location.hostname === 'localhost'
  ? '/.netlify/functions/rag'
  : 'https://unknownhands.netlify.app/.netlify/functions/rag';

function appendLabel(parent, label) {
  const strong = document.createElement('strong');
  strong.textContent = label;
  parent.appendChild(strong);
}

function appendUserMessage(chatMessages, message) {
  const userDiv = document.createElement('div');
  userDiv.className = 'user-message';
  appendLabel(userDiv, 'You:');
  userDiv.appendChild(document.createTextNode(' ' + message));
  chatMessages.appendChild(userDiv);
}

function appendBotMessage(chatMessages, data) {
  const botDiv = document.createElement('div');
  botDiv.className = 'bot-message';
  appendLabel(botDiv, 'Assistant:');
  botDiv.appendChild(document.createElement('br'));

  const markdownDiv = document.createElement('div');
  markdownDiv.className = 'rendered-markdown';
  const answer = data.answer || "Sorry, I couldn't generate an answer.";
  const rendered = marked.parse(answer);
  markdownDiv.innerHTML = DOMPurify.sanitize(rendered);
  botDiv.appendChild(markdownDiv);

  if (Array.isArray(data.sources) && data.sources.length > 0) {
    const sourceDiv = document.createElement('div');
    sourceDiv.className = 'source-badges';
    appendLabel(sourceDiv, 'Referenced Manuscripts:');
    sourceDiv.appendChild(document.createElement('br'));
    data.sources.forEach((source) => {
      const badge = document.createElement('span');
      badge.className = 'source-badge';
      badge.textContent = source.title || source.id || 'Untitled source';
      sourceDiv.appendChild(badge);
    });
    botDiv.appendChild(sourceDiv);
  }

  chatMessages.appendChild(botDiv);
}

function appendErrorMessage(chatMessages, error) {
  const isRateLimit = error.message.includes("high traffic") || error.message.includes("rate limit");
  const errorDiv = document.createElement('div');
  errorDiv.className = 'bot-message';
  errorDiv.style.backgroundColor = isRateLimit ? '#fff3cd' : '#f8d7da';
  errorDiv.style.color = '#721c24';
  appendLabel(errorDiv, isRateLimit ? 'System Notice:' : 'Error:');
  errorDiv.appendChild(document.createTextNode(' ' + error.message));
  chatMessages.appendChild(errorDiv);
}

async function sendMessage() {
  const input = document.getElementById('user-input');
  const message = input.value.trim();
  if (!message) return;
  
  // Clear input
  input.value = '';
  
  const chatMessages = document.getElementById('chat-messages');
  
  appendUserMessage(chatMessages, message);
  
  // Show loading
  document.getElementById('loading-indicator').style.display = 'block';
  chatMessages.scrollTop = chatMessages.scrollHeight;
  
  try {
    const response = await fetch(RAG_API_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ query: message })
    });
    
    // Add explicitly handled error checking!
    if (!response.ok) {
      if (response.headers.get('content-type')?.includes('text/html')) {
        throw new Error("Server timeout or Netlify gateway error (Status: " + response.status + ")");
      }
      const errData = await response.json().catch(() => ({}));
      throw new Error(errData.error || "Failed to communicate with the semantic engine.");
    }

    const data = await response.json();
    
    document.getElementById('loading-indicator').style.display = 'none';
    
    appendBotMessage(chatMessages, data);
    
  } catch (error) {
    console.error(error);
    document.getElementById('loading-indicator').style.display = 'none';
    appendErrorMessage(chatMessages, error);
  }
  
  chatMessages.scrollTop = chatMessages.scrollHeight;
}
</script>
